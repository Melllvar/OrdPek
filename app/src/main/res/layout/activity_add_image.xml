<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context="se.henkan.ordpek.AddImageActivity">

    <LinearLayout
        android:orientation="vertical"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content">

        <TextView
            android:text="@string/add_image_title"
            android:layout_width="fill_parent"
            android:layout_height="wrap_content" />

        <Button
            android:layout_width="fill_parent"
            android:layout_height="50dp"
            android:text="Add from gallery"
            android:onClick="addFromGallery"/>

        <Button
            android:layout_width="fill_parent"
            android:layout_height="50dp"
            android:text="Add from camera"
            android:onClick="addFromCamera"/>

        <Button
            android:layout_width="fill_parent"
            android:layout_height="50dp"
            android:text="List all images"
            android:onClick="listAllImages"/>

        <TextView
            android:id="@+id/add_text_view"
            android:text="text"
            android:layout_width="fill_parent"
            android:layout_height="0dp"
            android:lines="10"
            android:layout_weight="2"
            android:scrollbars = "vertical"/>

        <ImageView
            android:id="@+id/add_image_view"
            android:layout_width="wrap_content"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:src="@drawable/op_agnes"/>

    </LinearLayout>

</RelativeLayout>
